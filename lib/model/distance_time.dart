class DistanceAndTime {
  final double totalDistance;
  final int totalTime;
  Map<String,DistanceAndTime>distanceCache={};

  DistanceAndTime({required this.totalDistance, required this.totalTime});
}